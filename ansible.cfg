[defaults]
home = /local/ansible
inventory = /local/ansible/inventory
private_key_file = /home/ansible/.ssh/id_ed25519
playbook_dir = /local/ansible/playbooks
remote_user = ansible

[ssh_connection]
ssh_args = -o StrictHostKeyChecking=accept-new
